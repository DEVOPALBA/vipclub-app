VIPCLUB PWA

Upload all files from this folder to one web-server directory. Keep the existing guestlist JSON in its current subfolder: bulcpiv/alba-event-data-7f31c2.json

Open the app via HTTPS (or localhost for testing), not via file://, so installation and the service worker work.

Password: vipclub

Hinweis: Der Passwortschutz ist clientseitig und daher kein sicherer Schutz für vertrauliche Daten. Für echte Zugriffskontrolle muss auch der Webserver bzw. die JSON-Datei geschützt werden.
